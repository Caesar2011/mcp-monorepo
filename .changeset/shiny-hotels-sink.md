---
'@mcp-monorepo/notion-query': minor
---

add fetch tool to retrieve notion pages/databases and format responses to markdown; introduce client singleton and id normalization; register tool in server and update dependencies
