[
  {
    "name": "`CALENDAR_<NAME>`",
    "description": "Defines a calendar source. Replace `<NAME>` with a unique identifier (e.g., `WORK`, `PERSONAL`). The value must be a valid URL to an `.ics` file. You can define multiple `CALENDAR_` variables to merge events from several sources. At least one is required.",
    "default": "<required>"
  }
]
